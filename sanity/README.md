# Sanity Studio for Qeyafa

## Quickstart
```bash
npm install -g @sanity/cli
sanity init --dataset production --project "qeyafa" --create-project "Qeyafa CMS" --template clean
cd sanity
npm install
npm run dev
```

